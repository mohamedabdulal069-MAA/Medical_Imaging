# Preserved archive contents

These are unchanged historical inputs, not the repaired runnable implementation. They contain known leakage, preprocessing, evaluation, and inference defects. Do not reuse their scores or model files as validated experimental results. Run the `ich` package at the workspace root instead. File and archive hashes are in `audit/inventory.json`.

